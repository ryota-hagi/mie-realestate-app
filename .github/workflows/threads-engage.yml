name: Threads Business (投稿 + エンゲージメント)

on:
  schedule:
    - cron: '0 * * * *'  # 毎時実行、投稿/エンゲージの判定はジョブ内で行う
  workflow_dispatch:
    inputs:
      force_job:
        description: '強制実行するジョブ'
        required: false
        type: choice
        options:
          - auto
          - post
          - engage

concurrency:
  group: threads-business
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  post:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check posting time
        id: check
        run: |
          HOUR=$(TZ=Asia/Tokyo date +%H)
          FORCE="${{ github.event.inputs.force_job }}"
          echo "🕐 現在 JST ${HOUR}時"
          if [ "$FORCE" = "post" ] || [ "$FORCE" = "auto" ]; then
            echo "✅ 強制実行: post"
            echo "should_run=true" >> $GITHUB_OUTPUT
          elif echo "14 19" | grep -qw "$HOUR"; then
            echo "✅ 投稿対象の時間帯です"
            echo "should_run=true" >> $GITHUB_OUTPUT
          else
            echo "⏭️ 投稿対象外 → スキップ"
            echo "should_run=false" >> $GITHUB_OUTPUT
          fi

      - uses: actions/checkout@v4
        if: steps.check.outputs.should_run == 'true'

      - uses: actions/setup-node@v4
        if: steps.check.outputs.should_run == 'true'
        with:
          node-version: '20'

      - name: Post to Threads (Business)
        if: steps.check.outputs.should_run == 'true'
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          THREADS_ACCESS_TOKEN: ${{ secrets.THREADS_ACCESS_TOKEN }}
          THREADS_USER_ID: ${{ secrets.THREADS_USER_ID }}
        run: node scripts/threads/poster.mjs --account=business

      - name: Commit posting history
        if: steps.check.outputs.should_run == 'true'
        run: |
          git pull --rebase || true
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/threads-history.json data/threads-trends.json
          git diff --staged --quiet || git commit -m "chore: threads post [$(date -u +%Y-%m-%d)]"
          for i in 1 2 3; do
            git push && break
            echo "Push failed (attempt $i), retrying..."
            sleep $((RANDOM % 10 + 5))
            git pull --rebase || true
          done

  engage:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: post
    if: always()

    steps:
      - name: Check engage time
        id: check
        run: |
          HOUR=$(TZ=Asia/Tokyo date +%H)
          FORCE="${{ github.event.inputs.force_job }}"
          echo "🕐 現在 JST ${HOUR}時"
          if [ "$FORCE" = "engage" ] || [ "$FORCE" = "auto" ]; then
            echo "✅ 強制実行: engage"
            echo "should_run=true" >> $GITHUB_OUTPUT
          elif [ "$HOUR" = "18" ]; then
            echo "✅ エンゲージメント実行時間です"
            echo "should_run=true" >> $GITHUB_OUTPUT
          else
            echo "⏭️ エンゲージメント対象外 → スキップ"
            echo "should_run=false" >> $GITHUB_OUTPUT
          fi

      - uses: actions/checkout@v4
        if: steps.check.outputs.should_run == 'true'

      - uses: actions/setup-node@v4
        if: steps.check.outputs.should_run == 'true'
        with:
          node-version: '20'

      - name: Search and engage with housing threads
        if: steps.check.outputs.should_run == 'true'
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          THREADS_ACCESS_TOKEN: ${{ secrets.THREADS_ACCESS_TOKEN }}
          THREADS_USER_ID: ${{ secrets.THREADS_USER_ID }}
        run: node scripts/threads/engager.mjs

      - name: Commit engagement history
        if: steps.check.outputs.should_run == 'true'
        run: |
          git pull --rebase || true
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/threads-history.json
          git diff --staged --quiet || git commit -m "chore: threads engage [$(date -u +%Y-%m-%d)]"
          for i in 1 2 3; do
            git push && break
            echo "Push failed (attempt $i), retrying..."
            sleep $((RANDOM % 10 + 5))
            git pull --rebase || true
          done
